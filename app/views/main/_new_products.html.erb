<% products.each do |product| %>
  <li><%= link_to image_tag(product.image_url), product_path(product.id) %>
      <%= link_to product.name, product_path(product.id) %>
      <%= product.price %></li>
  <% end %>
