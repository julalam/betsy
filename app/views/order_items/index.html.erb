<h1> Shopping Cart </h1>

<% if @order_items %>
<%= render partial: "item_summary" %>

<%=link_to "Submit order", edit_order_path(@order_items.first.order_id), class: "button"%>

<% else  %>
<p>
The shopping cart is empty
</p>

<%end%>
